# AtilKurt - Copy to .env and set your values (do not commit .env)
ATILKURT_DOMAIN=corp.local
ATILKURT_USER=admin
ATILKURT_PASS=YourSecurePassword
# Optional when the domain name resolves to a domain controller
ATILKURT_DC_IP=10.0.0.1
ATILKURT_OUTPUT=report.html
